```

**What ships:**

- **Group members are the primary path** — a clean scrollable list with avatars (gradient + initials, deterministic per-wallet), nicknames, and short addresses
- **Search bar appears when ≥5 members** — filters by name or address
- **Manual input collapsed by default** — "Send to someone outside" toggle reveals the address/ENS field
- **Selected recipient card** — shows avatar, name, address with a one-tap "Change" button
- **Smart defaults** — opens with manual input pre-toggled if no group context; otherwise members list is front-and-center
- **No layout regression** — full-screen sheet on mobile, modal on desktop, same chain selector and amount UX

Sending USDC to "Bob" is now: open dialog → tap Bob → enter amount → send. 🚀